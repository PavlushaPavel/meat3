/**
 * Тексты бейджа допуска (docs/SPEC.md §3.7).
 *
 * Прогресс воронки, показанный предметно: статус на карточке-пропуске, а не
 * проценты и не абстрактная шкала. Статус меняется в момент перехода акта —
 * поэтому здесь ровно шесть значений, по одному на акт (`ActId` из
 * `src/acts.ts`), от «посторонний» в начале до «допущен» после теста
 * (те же два слова, которыми это описано в каноне).
 *
 * Компонент (`src/ui/Badge.tsx`) не придумывает тексты сам — читает их
 * отсюда, как и весь остальной копирайт живёт в `src/content/*`.
 */
import type { ActId } from '@/acts';

/** Легенда карточки — служебная подпись над статусом, одна на все акты. */
export const BADGE_LEGEND = 'ДОПУСК';

/** Статус пропуска на каждом акте. */
export const BADGE_STATUS: Record<ActId, string> = {
  i: 'ПОСТОРОННИЙ',
  ii: 'БЕЗ ДОПУСКА',
  iii: 'ПОД НАБЛЮДЕНИЕМ',
  iv: 'В ОБРАБОТКЕ',
  v: 'НА ПРОВЕРКЕ',
  vi: 'ДОПУЩЕН',
};
