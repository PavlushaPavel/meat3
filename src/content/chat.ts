/**
 * Шаг 4. Входная боль: переписка с клиентом.
 *
 * ЗДЕСЬ МИР ВЫКЛЮЧАЕТСЯ. Ни жёлтой ленты, ни неона, ни металла — обычный
 * тёмный мессенджер (docs/SPEC.md §3.3).
 *
 * САМИ СООБЩЕНИЯ ЖИВУТ В `src/content/districts.ts`: у директолога клиент
 * говорит про заявки и РОПа, у авитолога — про контакты и чаты, у таргетолога —
 * про номера с лид-формы. Здесь только то, что общее для всех трёх.
 *
 * ЧТО ИЗМЕНИЛОСЬ 08.08.2026. Раньше клиент писал «лиды говно» и стирал первые
 * сообщения. Теперь он не стирает ничего и доводит до конца: останавливает
 * рекламу, прекращает сотрудничество и просит вернуть остаток оплаты. Ставка
 * выросла с «тебя ругают» до «ты теряешь клиента и деньги» — и печать в финале
 * бьёт именно поэтому.
 */

/** Кто пишет. Имя намеренно безликое: это любой клиент, а не конкретный. */
export const chatHeader = {
  name: 'Клиент',
  status: 'печатает…',
  seen: 'был(а) недавно',
} as const;

/**
 * Печать поверх всего экрана после того, как переписка замерла.
 *
 * РОЛЬ СТОИТ ВНУТРИ ФРАЗЫ, А НЕ ПОД НЕЙ. Сначала печать читалась «ВО ВСЁМ
 * ВИНОВАТ ТЫ», а роль шла отдельной строкой ниже — получалось обвинение вообще
 * плюс подпись мелким шрифтом. Подставленная в саму фразу
 * (ДИРЕКТОЛОГ / АВИТОЛОГ / ТАРГЕТОЛОГ) она бьёт по профессии, а не по человеку
 * вообще: именно так это и звучит от клиента.
 *
 * Роль берётся из района (`districtCopy(id).role`) — здесь её нет и быть не
 * должно.
 */
export const blameStamp = {
  prefix: 'ВО ВСЁМ ВИНОВАТ',
  cta: 'Разобраться',
} as const;

/**
 * Кнопка пропуска обязательна: человек может открыть воронку второй раз, и
 * заставлять его пересматривать чужое раздражение — это неуважение.
 */
export const chatSkip = 'Показать всё сразу';
