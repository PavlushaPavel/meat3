import type { EmptySiteContent } from './types';
import { linkBreakContent } from './linkbreak';

/**
 * Экран 6 — `empty-site` (SPEC.md §4). Id вариантов ответа — говорящие
 * латиницей, сохраняются в `siteAnswer` (SPEC.md §6).
 *
 * `headline` — правка координатора после ревизии задачи 3: дословно та же
 * строка, что открывает карточку сайта на экране 10 (`linkBreakContent.site[0]`,
 * см. `linkbreak.ts`) — это один и тот же сайт, и рифма («забраковал этот
 * сайт на экране 6 → попал на него же после клика на экране 10») держится
 * только на буквальном совпадении текста. Берём значение напрямую из
 * `linkbreak.ts`, а не дублируем строкой: если оно когда-нибудь изменится на
 * экране 10, здесь оно останется идентичным автоматически.
 */
export const emptySiteContent: EmptySiteContent = {
  headline: linkBreakContent.site[0],
  bullets: ['Высокое качество', 'Индивидуальный подход', 'Команда профессионалов', 'Работаем на результат'],
  question: 'Ты бы здесь купил?',
  options: [
    { id: 'yes', label: 'Да' },
    { id: 'no', label: 'Нет' },
    { id: 'unclear', label: 'Я вообще не понял, что продают' },
  ],
  button: 'Смотреть видео',
};
